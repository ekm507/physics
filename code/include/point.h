//point properties

#include "types.h"

// this class is used to define a point in 2D space
class point
{
	position_type x;
	position_type y;
	//(x,y)
	public:
		point(position_type, position_type,);
		//set point's place. (x,y).
};

point::point(position_type left,position_type top)
{
	x=left;
	y=top;
}

